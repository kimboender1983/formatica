// ---------------------------------------------------------------------------
// Formatica Vue – Validation rule registry (re-exports from core)
// ---------------------------------------------------------------------------

// Import core to trigger built-in rule registration, then re-export
export { getRule, hasRule, registerRule, unregisterRule } from "@formatica/core";
